# --- Core Application Framework and ASGI Server ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==21.2.0

# --- Scientific Computing and Data Handling (Required by quantum_core.py, qsh_engind.py) ---
numpy==1.26.4
pydantic==2.7.4  # For FastAPI models and validation
python-multipart==0.0.9 # For file uploads

# --- Security and Cryptography (Required for SecurityManager in init.py) ---
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
cryptography==42.0.8

# --- Database (Inferred from main-2.py's imports, likely for SQLAlchemy) ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9

# --- Communication and Networking ---
websockets==13.1 # For P2PManager and real-time updates

# --- Logging (Inferred from common practice and log configuration in main-2.py) ---
# python-json-logger # Optionally, for JSON formatted logs, if used explicitly.
